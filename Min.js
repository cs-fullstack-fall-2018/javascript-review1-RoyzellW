/*   First step consists of myself creating an array which is a collection of items assigned to a certain variable and given an index.
     The items in the repository of whatever variable it is assigned to can consit of intergers(numbers) or strings(words).
     Considering that each item in the array has an indecx you can print a specific item by using it's index.
     You can use tha index to print out all of them specifically.
 */

var squad = ["one", 'two', "three", "four", "five"];

document.write(squad[4], squad[3], squad[2], squad[1], squad[0]);
